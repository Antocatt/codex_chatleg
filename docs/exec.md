# Non-interactive mode

For information about non-interactive mode, see [this documentation](https://developers.openai.com/codex/noninteractive).

## Events Reference

### Exec Mode Events

For a complete reference of all JSON events emitted by `codex exec` mode (non-interactive JSONL output), see [Exec Mode JSON Events Reference](./exec-mode-json-events.md).

### TUI Mode Events

For a complete reference of all events used by the regular Codex TUI (Terminal User Interface) mode, see [TUI Mode Events Reference](./tui-mode-events.md).
